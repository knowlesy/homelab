# VM Passwords Configuration
# =========================
# This file contains sensitive password information.
# Never commit this file to version control!

# Default password for all VMs (if individual passwords not specified)
default_vm_password = "YourSecurePassword123!"

# Individual VM passwords (optional - will use default_vm_password if not specified)
vm_passwords = {
  vm1 = "WebServerPassword123!"     # homelab-web-01
  vm2 = "DatabasePassword456!"      # homelab-db-01
  vm3 = "AppServerPassword789!"     # homelab-app-01
}

# Note: You can use either:
# 1. Set default_vm_password for all VMs to use the same password
# 2. Set individual passwords in vm_passwords map
# 3. Mix both - individual passwords override the default
